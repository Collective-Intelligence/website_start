<div
    class="card module"
    id="module-{{id}}"
    onmousedown="_ci.ui.moduleDrag.mouseDown(event)"
    moduleID="{{id}}"
>
    <div class="module__content">
        <p>{{name}}</p>
    </div>
</div>
